<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="de.ka.chappted.main.screens.challenges.HeaderItemViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="6dp">
        
        <View
            android:id="@+id/categoryImage"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_marginBottom="4dp"
            android:layout_marginStart="10dp"
            android:layout_alignParentBottom="true"
            android:background="@drawable/skeleton_text_light"/>

        <TextView
            style="@style/DefaultHeaderStyle"
            android:layout_width="match_parent"
            android:layout_toEndOf="@+id/categoryImage"
            android:layout_alignParentBottom="true"
            android:layout_marginStart="4dp"
            android:layout_height="22dp"
            android:ellipsize="end"
            android:gravity="bottom"
            tools:text="Recommended"
            android:maxLines="1"
            android:text="@{viewModel.challenge.category}" />

        <Button
            android:layout_alignParentEnd="true"
            style="@style/DefaultButtonStyle"
            android:onClick="@{(v) -> viewModel.submit()}"
            android:layout_width="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_height="22dp"
            android:text="@string/show_more" />

    </RelativeLayout>
</layout>